type CoreParams {
  _id: String
  id: Int
  externalCode: String
  name: String
  parentId: Int
  type: String
}

extend type Query {
  getCoreParams(type: String!, name: String, externalCode: String, parentId: Int): [CoreParams]
}
