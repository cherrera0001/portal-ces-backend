{
	"paths": {
		"/checkList": {
			"get": {
				"tags": ["checkList"],
				"summary": "Gets a  list of documents required by a financial entity.",
				"description": "",
				"operationId": "getCheckList",
				"consumes": ["application/json"],
        "produces": ["application/json"],
        "parameters": [
          {
            "name": "x-api-key",
            "in": "header",
            "required": true,
            "type": "string"
          }
				],
        "responses": {
          "200": {
            "description": "Successful operation",
            "schema": {
              "$ref": "#/definitions/CheckList"
            }
          },
          "403": {
            "description": "Error: Forbidden"
          },
          "401": {
            "description": "Unauthorized"
          }
        }
			}
		}
  },
  "definitions": {
    "CheckList": {
      "feName": {
        "type": "string",
        "example": "Santander",
        "required": false,
        "description": "Parameter name, meant to be shown to the user"
      },
      "checkList": {
        "type": "array",
        "items": {
          "properties": {
            "id": {
              "type": "integer",
              "example": 3984,
              "required": false,
              "description": "Parameter name, meant to be shown to the user"
            },
            "name": {
              "type": "string",
              "example": "Liquidaciones",
              "required": false,
              "description": "Parameter name, meant to be shown to the user"
            }
          }
        }
      }
    }
  }
}
